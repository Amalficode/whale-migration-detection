# 🐋 Whale Migration Detection

Проект по исследованию миграции китов с помощью компьютерного зрения.

Цель — автоматически находить китов на фотографиях и определять их вид
(серый кит — *gray whale* или горбатый кит — *humpback whale*).
Это может использоваться в экологическом мониторинге и при анализе
миграционных маршрутов.

## Почему важно изучать миграцию китов

- киты находятся на вершине морских пищевых цепей и отражают состояние всей экосистемы;
- изменение маршрутов миграции показывает влияние климата и человеческой активности;
- данные о местах кормления и размножения помогают создавать охраняемые зоны;
- раннее обнаружение снижения численности позволяет предотвратить исчезновение вида.

Автоматический анализ изображений снижает ручной труд исследователей и
помогает быстрее собирать статистику наблюдений.

## Структура репозитория

```text
whale-migration-detection/
├── README.md
├── requirements.txt
├── inference.py        # простой скрипт для запуска модели
├── whale_migration_notebook.ipynb
├── examples/           # примеры входных изображений
└── results/            # сюда сохраняются результаты
```

## Как запустить локально

1. Установить зависимости:

```bash
pip install -r requirements.txt
```

2. Открыть файл `inference.py` и указать:

   - `API_URL` — URL workflow из Roboflow
   - `API_KEY` — ваш API‑ключ Roboflow

3. В `inference.py` задать путь к изображению `IMAGE_PATH`
   и запустить скрипт:

```bash
python inference.py
```

На выходе появится файл с рамкой и подписью вида кита в папке `results/`.

## Notebook

Файл `notebook_template.ipynb` содержит заготовку для анализа данных
в Google Colab или Jupyter.

Вы можете заменить этот файл своим рабочим ноутбуком из курса.

---
Автор: Natalia Brodo
